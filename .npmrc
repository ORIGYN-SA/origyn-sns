# @origyn/shared-ui comes from the shared-ui GitLab package registry.
# No auth needed: gitlab.bity.com is Tailscale-only and the shared-ui project
# allows anonymous package pulls (see its README for the fallback if disabled).
@origyn:registry=https://gitlab.bity.com/api/v4/projects/bity%2Fdev%2Ficp%2Forigyn%2Fshared-ui/packages/npm/
