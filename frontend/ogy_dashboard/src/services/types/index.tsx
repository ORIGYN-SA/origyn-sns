import { IListProps } from "@services/types/list";
import {
  IProposalResult,
  IProposalData,
  IProposalVotes,
} from "@services/types/proposals";

import {
  INeuronResult,
  INeuronData,
  IDissolveState,
} from "@services/types/neurons";

export type {
  IListProps,
  IProposalResult,
  IProposalData,
  IProposalVotes,
  INeuronResult,
  INeuronData,
  IDissolveState,
};
